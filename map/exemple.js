/*
const persons = ['Iron Man', 'Black Widow', 'Thor'];

const personsWithPosition = persons.map(
  (personName, index) => `${index + 1}. ${personName}`
);

console.log(personsWithPosition);

const numbers = [2, 4, 7];
const doubledNumbers = numbers.map(num => 2 * num);
console.log(doubledNumbers);

// écriture "traditionnelle" en fonction nommée
function double1(num) {
  return 2 * num;
}

// fonction anonyme qu'on affecte à une constante
const double2 = function(num) {
  return 2 * num;
}

// passage en fonction fléchée
const double3 = (num) => {
  return 2 * num;
}

// uniquement dans le cas où la fonction n'a qu'une ligne
// avec un return
const double4 = num => 2 * num;
  

console.log(double1(3));
console.log(double2(3));
*/